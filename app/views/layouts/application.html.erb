<!DOCTYPE html>
<html lang="fr">
  <head>
    <title>Okapi</title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <nav class="red accent-4">
        <div class="container nav-wrapper">
          <a class="brand-logo right" href="/">Okapi</a>
          <a href="#" data-activates="mobile-demo" class="button-collapse"><i class="material-icons">menu</i></a>
          <ul id="dropdown-categorie" class="dropdown-content">
            <% @menu_categories.each do |categorie| %>
              <li><a href="/category/<%= categorie["id"] %>"><%= categorie["title"].split(" ")[2] %></a></li>
              <li class="divider"></li>
            <% end %>
          </ul>
          <ul id="nav-mobile" class="left hide-on-med-and-down">
            <li><a href="/">Home</a></li>
            <% if user_signed_in? %>
              <li><a class="dropdown-button" href="#!" data-activates="dropdown-categorie">Categories<i class="material-icons right">arrow_drop_down</i></a></li>
              <li><a rel="nofollow" data-method="delete" href="/users/sign_out">Log out</a></li>
            <% else %>
              <li><a href="/users/sign_up">Sign up</a></li>
              <li><a href="/users/sign_in">Login</a></li>
            <% end %>
          </ul>
          <ul class="side-nav" id="mobile-demo">
            <li><a href="/">Home</a></li>
            <% if user_signed_in? %>
              <li><a class="dropdown-button" href="#!" data-activates="dropdown-categorie">Categories<i class="material-icons right">arrow_drop_down</i></a></li>
              <li><a rel="nofollow" data-method="delete" href="/users/sign_out">Log out</a></li>
            <% else %>
              <li><a href="/users/sign_up">Sign up</a></li>
              <li><a href="/users/sign_in">Login</a></li>
            <% end %>
          </ul>
        </div>
      </nav>
    </header>
    <main class="container">
      <div class="row">
        <% if notice %>
          <div class="card-panel">
            <span class="notice light-blue-text accent-3"><%= notice %></span>
          </div>
        <% end %>
        <% if alert %>
          <div class="card-panel">
            <span class="alert light-blue-text accent-3"><%= alert %></span>
          </div>
        <% end %>
        <%= yield %>
      </div>
    </main>
    <footer>
    </footer>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  </body>
</html>
